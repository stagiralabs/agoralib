import VerifiedAgora.tagger

-- Moderate challenges
@[target]
theorem final_list_length {α : Type} (xs : List α) : xs.length = xs.length := by
  sorry

@[target]
theorem final_nat_prop (n : Nat) : n * 1 = n := by
  sorry

@[target]
theorem final_add_assoc (a b c : Nat) : (a + b) + c = a + (b + c) := by
  sorry
